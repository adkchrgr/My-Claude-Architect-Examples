## Scope
We are learning about Claude Agent SDK

## Requirements
-Must use claude-agent-sdk
-Python
-lib/sdk_parser to format messaging

## Goal

We are creating code examples to learn about stop_reason
We want to show the user how stop_reason works within the Claude Agent SDK.
We need to see stop_reason for tool_user
We need to stop_reason for end_result
We need to see how the results is appended
I want to use a weather tool_use example.
We are implementing in Python
We want to use the lib/sdk_parser to format logging information
